spring:
  profiles:
    default: local
  application:
    name: trichter-backend
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${BETTERAUTH_URL}/api/auth/.well-known/jwks.json
          issuer-uri: ${BETTERAUTH_URL}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  build-id: "@buildId@"
  device:
    secret: ${DEVICE_SECRET:dev-secret}
  storage:
    azure:
      container-name: trichter-image
      key-prefix: runs/images

springdoc:
  swagger-ui:
    path: /api/v2/docs
  api-docs:
    path: /api/v2/api-docs
  show-actuator: true

logging:
  level:
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn
