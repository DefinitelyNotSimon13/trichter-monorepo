spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trichterdb
    username: postgres
    password: password

# Override JWT config for local development; change to match your local BetterAuth instance
BETTERAUTH_URL: http://localhost:3000

app:
  cors:
    allowed-origins: http://localhost:5173
