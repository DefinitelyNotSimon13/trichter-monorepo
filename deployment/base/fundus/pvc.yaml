apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fundus-postgres-data
  namespace: fundus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
